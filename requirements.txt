# Mantido por conveniência/compatibilidade. A fonte da verdade das
# dependências é `pyproject.toml` (`pip install -e .` ou `pip install -e ".[dev]"`).
pydantic>=2.0,<3.0
pandas>=2.0
numpy>=1.24
scipy>=1.10
pyyaml>=6.0
networkx>=3.0
scikit-learn>=1.3
matplotlib>=3.7
pypdf>=5.0
pytest>=7.4
